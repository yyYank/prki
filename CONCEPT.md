# CONCEPT - prki (PR木) Design Philosophy

## Core Concept

**prki（PR木）** は「すでに大きくなったPRを救済する」ためのツールです。

### 名前の由来

- **PR Tree** → **PR木** → **prki**（日本語読み）
- 親PRを根として、子PRが枝分かれする「木構造」を表現

## Problem Statement

### AIコーディング時代の新しい問題

従来のソフトウェア開発では、開発者が慎重に計画を立て、小さな単位でコミットすることが推奨されてきました。しかし、AIアシスタント（Claude Code, Cursor, GitHub Copilot等）の登場により、状況が一変しました。

#### AIコーディングの特徴:

1. **爆速実装**
   - 「決済機能作って」→ 数分で2000行実装
   - 人間より圧倒的に速い

2. **過剰な実装**
   - 頼んだ機能 + ついでに関連機能も実装
   - エラーハンドリング、テスト、ドキュメントまで一気に
   - 「ちょっと修正」→ リファクタも勝手に実施

3. **予測困難**
   - どこまで実装されるか事前に分からない
   - 気づいたら巨大なdiff

#### 結果: 巨大PR問題

```
開発者: 「ログイン機能追加して」

AI: 「はい！実装しました！」
    ├─ 認証ロジック (500行)
    ├─ セッション管理 (300行)
    ├─ UIコンポーネント (400行)
    ├─ エラーハンドリング (200行)
    ├─ バリデーション (150行)
    ├─ テスト (300行)
    └─ ドキュメント (150行)
    
    合計: 23ファイル, 2000行

開発者: 「えっ...レビュー依頼どうしよう...」
レビュアー: 「でかすぎて見れない」
```

### 既存ツールでは解決できない

#### Stacked PR ツール（Graphite, SPR, stack-pr）の限界:

これらのツールは「**最初から小さく作る**」ことを前提としています:

```bash
# Graphite/SPR/stack-prの想定ワークフロー
git commit -m "Add auth endpoint"          # PR1
git commit -m "Add session management"     # PR2  
git commit -m "Add UI component"           # PR3
```

**問題点:**
- AIは「小さく作る」を理解しない
- AIは一気に全部実装する
- 事後的に分割できない

**ユースケースの違い:**

| | 既存ツール | prki |
|---|---|---|
| タイミング | 最初から計画 | すでに大きい |
| 粒度 | コミット単位 | 意味のかたまり |
| 構造 | 垂直スタック | 水平分割 |
| AI対応 | × | ◎ |

## Solution: Parent-Child PR Architecture

### 親子PR構造

```
main
 ↑
 │ 親PR #100: feature/payment-system (全体)
 │
feature/payment-system
 ↑         ↑         ↑
 │         │         │
 │         │         └─ 子PR #103: review/tests
 │         └─────────── 子PR #102: review/core
 └───────────────────── 子PR #101: review/config
```

### ワークフロー

#### 1. 分析フェーズ

```bash
$ prki analyze

🌳 PR木分析中...

現在の変更: 23ファイル, 1847行

分割案:
  ├─ Infrastructure & Config (5ファイル, 200行)
  │   - package.json, tsconfig.json
  │   - .github/workflows
  │   - 複雑度: 低
  │
  ├─ Core Business Logic (12ファイル, 1200行)
  │   - src/services/payment.ts
  │   - src/models/transaction.ts
  │   - 複雑度: 高
  │
  └─ Tests & Docs (6ファイル, 447行)
      - *.test.ts
      - README.md
      - 複雑度: 低

推奨: 3つの子PRに分割
理由: レビュー時間 60分 → 15分×3 に短縮
```

#### 2. 分割フェーズ

```bash
$ prki split

実行内容:
1. 子ブランチを作成
   - review/config (feature/payment-systemから分岐)
   - review/core (feature/payment-systemから分岐)
   - review/tests (feature/payment-systemから分岐)

2. 各ブランチに該当ファイルのみコミット
   - Authorは元のコミット作成者を保持
   - コミットメッセージはユーザーが指定

3. 子PRを作成
   - Base: feature/payment-system
   - Head: review/xxx
   - Draft状態で作成

続行しますか? [Y/n] y

✓ ブランチ review/config 作成
✓ ブランチ review/core 作成  
✓ ブランチ review/tests 作成

コミットメッセージを入力してください:

review/config: Add infrastructure and config files
review/core: Implement payment core logic
review/tests: Add tests and documentation

✓ 子PR #101 作成: [Review] Infrastructure & Config
✓ 子PR #102 作成: [Review] Core Business Logic
✓ 子PR #103 作成: [Review] Tests & Docs

完了！次のステップ:
1. 各子PRをレビュー依頼
2. 承認後: prki merge
```

#### 3. レビューフェーズ

**レビュアーの体験:**

```
従来（巨大PR）:
- 23ファイル、1847行を一度に見る
- コンテキストスイッチが多い
- 疲れる、見落とす
- スマホで見れない

prki使用後（子PR）:
- PR #101: 設定だけ（5ファイル、200行）
- PR #102: コアロジックだけ（12ファイル、1200行）
- PR #103: テストだけ（6ファイル、447行）

→ 各PRが独立して理解可能
→ スマホでも見れる
→ 集中してレビューできる
```

#### 4. マージフェーズ

```bash
$ prki status

親PR: #100 feature/payment-system
  ├─ 子PR #101: review/config [approved ✓]
  ├─ 子PR #102: review/core [approved ✓]
  └─ 子PR #103: review/tests [approved ✓]

全ての子PRが承認されました！

$ prki merge

✓ PR #101 を feature/payment-system にマージ
✓ PR #102 を feature/payment-system にマージ
✓ PR #103 を feature/payment-system にマージ

親PR #100 の差分を確認:
$ git diff main..feature/payment-system

差分: 統合部分のみ（コンフリクト解消など）
→ 最終レビュー依頼

親PR承認後、mainにマージして完了！
```

## Design Principles

### 1. 事後的救済（Retrospective Rescue）

**最初から完璧に計画することを要求しない**

- AIが実装した後でも分割可能
- 「気づいたら大きくなってた」を救済
- 現実的な開発フローに対応

### 2. 意味のかたまり（Semantic Grouping）

**コミット単位ではなく、意味のある単位で分割**

分割戦略:
- **ディレクトリ構造**: `src/auth/`, `src/payment/`
- **ファイルタイプ**: テスト/プロダクション/設定
- **セマンティック**: リファクタ/機能追加/バグ修正
- **複雑度**: 高複雑度のファイルは別PR

### 3. レビュアー優先（Reviewer-First）

**レビュアーの負担を最小化**

- 小さい単位で理解可能
- コンテキストが明確
- スマホでレビュー可能
- 並行レビュー可能（複数人で分担）

### 4. 非破壊的（Non-Destructive）

**元の開発ブランチは保持**

- 親ブランチ（feature/xxx）は変更しない
- 子ブランチは「ビュー」に過ぎない
- いつでも元に戻せる
- Git履歴を汚さない

### 5. 柔軟性（Flexibility）

**チームの運用に合わせてカスタマイズ可能**

- 分割戦略を選択可能
- 閾値をカスタマイズ可能
- PRテンプレートをカスタマイズ可能
- 自動化レベルを調整可能

## Technical Architecture

### Core Components

#### 1. Analyzer（分析エンジン）

**責務:** 変更を分析し、分割案を生成

```python
class Analyzer:
    def analyze(self, diff: GitDiff) -> SplitProposal:
        # 1. ファイル変更を取得
        files = self.get_changed_files(diff)
        
        # 2. グルーピング
        groups = self.group_files(files)
        
        # 3. 複雑度計算
        for group in groups:
            group.complexity = self.calculate_complexity(group)
        
        # 4. 依存関係分析
        dependencies = self.analyze_dependencies(groups)
        
        # 5. 分割案生成
        return SplitProposal(groups, dependencies)
```

#### 2. Splitter（分割実行）

**責務:** 分割案を実際のブランチ・PRに変換

```python
class Splitter:
    def split(self, proposal: SplitProposal) -> List[ChildPR]:
        child_prs = []
        
        for group in proposal.groups:
            # 1. 子ブランチ作成
            branch = self.create_child_branch(group)
            
            # 2. ファイルをコミット
            commit = self.commit_files(branch, group.files)
            
            # 3. PRを作成
            pr = self.create_pull_request(branch, group)
            
            child_prs.append(pr)
        
        return child_prs
```

#### 3. Merger（マージ管理）

**責務:** 子PRのマージ状態を管理

```python
class Merger:
    def merge_approved(self) -> MergeResult:
        # 1. 子PRの状態確認
        approved = self.get_approved_child_prs()
        
        # 2. 依存順にマージ
        for pr in self.topological_sort(approved):
            self.merge_to_parent(pr)
        
        # 3. 親PRの差分確認
        return self.check_parent_diff()
```

### Data Model

```python
@dataclass
class FileGroup:
    name: str                    # "Infrastructure & Config"
    files: List[str]            # ["package.json", "tsconfig.json"]
    lines_changed: int          # 200
    complexity: int             # 低=0-50, 中=51-100, 高=101+
    order: int                  # マージ順序
    
@dataclass  
class SplitProposal:
    groups: List[FileGroup]
    dependencies: Dict[str, List[str]]  # グループ間依存関係
    estimated_review_time: int          # 分単位
    
@dataclass
class ChildPR:
    number: int                 # PR番号
    branch: str                 # "review/config"
    group: FileGroup
    status: PRStatus            # draft/open/approved/merged
    parent_pr: int              # 親PR番号
```

## Use Cases

### Use Case 1: AIコーディング後の分割

**シナリオ:**
Claude Codeで機能実装 → 気づいたら2000行

**ワークフロー:**
```bash
# AIで実装
$ claude-code "決済機能を実装"

# 確認
$ git diff --stat
 23 files changed, 1847 insertions(+)

# 分析
$ prki analyze
→ 3つの子PRに分割可能

# 分割
$ prki split --auto

# レビュー
→ レビュアーが各子PRをレビュー

# マージ
$ prki merge
```

### Use Case 2: 途中で大きくなった場合

**シナリオ:**
少しずつ実装していたら、いつの間にか500行超え

**ワークフロー:**
```bash
# 開発中
$ git status
 modified: 15 files

# 警告
$ prki analyze --unstaged
⚠️  変更が大きいです

# 分割してコミット
$ prki split --unstaged
→ 意味のかたまりで複数ブランチに分割
```

### Use Case 3: レビュー指摘で分割

**シナリオ:**
PR出したら「でかすぎる」と言われた

**ワークフロー:**
```bash
# 既存のPRを指定
$ prki analyze --pr 123

# 分割
$ prki split

# 元のPR #123は親PRとして残る
# 新しい子PR #124, #125, #126 作成
```

## Comparison with Existing Tools

### vs Graphite/Charcoal

| Feature | prki | Graphite |
|---------|------|----------|
| **使用タイミング** | 事後（すでに大きい） | 事前（最初から小さく） |
| **分割単位** | 意味のかたまり | コミット |
| **PR構造** | 親子（水平） | スタック（垂直） |
| **学習コスト** | 低（gitの延長） | 中（新しいワークフロー） |
| **AI対応** | ◎ | △ |

### vs SPR

| Feature | prki | SPR |
|---------|------|-----|
| **1コミット=1PR** | × | ◎ |
| **ブランチ管理** | あり | なし |
| **フォーク対応** | ◎ | × |
| **柔軟性** | 高 | 低 |

### vs stack-pr

| Feature | prki | stack-pr |
|---------|------|----------|
| **分割タイミング** | 事後 | 事前 |
| **粒度調整** | 柔軟 | コミット固定 |
| **複雑度分析** | ◎ | × |

## Future Enhancements

### Phase 1: Core Features (v0.1-0.3)
- 基本的な分析・分割
- GitHub連携
- 複雑度計算

### Phase 2: AI Integration (v0.4-0.6)
- LLMによるセマンティック分析
  - 「このファイルはリファクタ」
  - 「このファイルは新機能」
- 自動コミットメッセージ生成
- レビューコメント自動反映

### Phase 3: Advanced Features (v0.7-1.0)
- GitLab/Bitbucket対応
- CI/CD統合
- チーム分析（誰が巨大PR作りがち？）
- レビュー時間予測

### Future Vision
- IDEプラグイン（VS Code, Cursor）
- リアルタイム分割提案
  - 「この変更、大きくなりそうです」
  - 「今のうちに分けますか？」
- AIエージェント統合
  - Claude Code: 実装時に自動分割

## Philosophy

**prki は「完璧」を求めない**

- 最初から完璧な計画は無理
- AIは予測不能
- 人間は間違える
- だから、事後的に救済する

**prki は「現実」に寄り添う**

- 理想: 小さいPRを計画的に
- 現実: 気づいたら大きくなってた
- prki: 大丈夫、分割できます

**prki は「レビュアー」を大切にする**

- レビュアーの時間は貴重
- スマホでレビューしたい
- 集中してレビューしたい
- prki: 小さく、わかりやすく

---

**prki (PR木)** - AIコーディング時代の救世主

